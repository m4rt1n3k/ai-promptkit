# Install design rule

Once per repo. Copy below the line.

---

## Task: Install design discipline rule

Create `.cursor/rules/design-discipline.mdc` (`alwaysApply: true`) from `subs/ai-promptkit/templates/design.rule.mdc`. Merge if exists. Do not commit unless asked.
